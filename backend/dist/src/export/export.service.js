"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ExportService = void 0;
const common_1 = require("@nestjs/common");
const graph_service_1 = require("../graph/graph.service");
const terraform_generator_1 = require("./generators/terraform.generator");
let ExportService = class ExportService {
    graphService;
    terraformGenerator;
    constructor(graphService, terraformGenerator) {
        this.graphService = graphService;
        this.terraformGenerator = terraformGenerator;
    }
    async generateIaC(projectId, versionNum, format) {
        const graph = await this.graphService.getFullGraph(projectId, versionNum);
        switch (format) {
            case 'terraform':
                return this.terraformGenerator.generate(graph);
            case 'ansible':
                return [
                    {
                        filename: 'playbook.yml',
                        content: this.generateAnsibleStub(graph),
                        language: 'yaml',
                    },
                ];
            default:
                throw new common_1.NotFoundException(`Format IaC "${format}" non supporté`);
        }
    }
    async exportJSON(projectId, versionNum) {
        const graph = await this.graphService.getFullGraph(projectId, versionNum);
        return [
            {
                filename: `architecture_${projectId}_v${versionNum}.json`,
                content: JSON.stringify(graph, null, 2),
                language: 'json',
            },
        ];
    }
    generateAnsibleStub(graph) {
        const hosts = graph.nodes
            .filter((n) => n.resourceType === 'virtual-machine' ||
            n.resourceType === 'physical-server')
            .map((n) => `    ${n.logicalName}:
      ansible_host: ${n.properties.ip || '{{ lookup("env", "HOST_IP") }}'}`)
            .join('\n');
        return `---
# Ansible inventory generated by ArchPlatform
# Project: ${graph.projectId}

all:
  hosts:
${hosts || '    # No compute instances found'}

  vars:
    ansible_user: deploy
    ansible_become: true
`;
    }
};
exports.ExportService = ExportService;
exports.ExportService = ExportService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [graph_service_1.GraphService,
        terraform_generator_1.TerraformGeneratorService])
], ExportService);
//# sourceMappingURL=export.service.js.map